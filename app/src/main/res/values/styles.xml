<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green4</item>
        <item name="colorPrimaryDark">@color/green4</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="home" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="register_input" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/login_hint_input_color</item>
        <item name="colorControlActivated">@color/login_hint_input_color</item>
    </style>
    
    
</resources>
